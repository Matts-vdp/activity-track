import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mattsvdp.activity',
  appName: 'activity-track',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
